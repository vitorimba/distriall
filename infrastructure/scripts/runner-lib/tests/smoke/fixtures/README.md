# Smoke Test Fixtures

Minimal inputs for validator smoke tests.

Validators use real squads/skills in the repo (e.g., `books` squad, `commit` skill)
so no synthetic fixtures are needed. This directory is reserved for future use
if a validator requires synthetic input that does not exist in the repo.

Story: 101.15
