# Story 5.2.1: Substituir RevenueChart (Recharts) por BarChart SVG do DS

## Status
Done

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
deploy_type: vercel

## Story
**As a** gestor do distribuidor,
**I want** ver o grafico de evolucao semanal usando o BarChart SVG do DS,
**so that** o visual seja consistente com o design system (cores --chart-*, tooltips, highlight do maximo).

## Acceptance Criteria
1. Dashboard usa `BarChart` de `@/components/ui/charts` no lugar de Recharts
2. Cores via tokens `--chart-1` (faturamento) e `--chart-3` (lucro)
3. Tooltip no hover mostra semana + valor formatado em BRL
4. Barra maxima destacada (opacity 1), demais a 0.5
5. Labels do eixo X mostram data da semana (dd/mm)

## Tasks / Subtasks
- [x] Task 1: Adaptar dados de WeeklyEvolution para formato {label, value}
- [x] Task 2: Substituir componente RevenueChart por BarChart do DS
- [x] Task 3: Verificar formatacao BRL no tooltip
- [x] Task 4: Testar com dados vazios (EmptyState)

## Effort
3 pontos

## Change Log
- 2026-06-12 @dev Done: Substituiu RevenueChart (Recharts) por dois BarChart SVG (revenue=--chart-1, profit=--chart-3). Labels do eixo X mapeados de ISO "YYYY-MM-DD" para "dd/mm". Tooltip formatado com fmtBRL. Barra máxima destacada via prop highlightMax. EmptyState com BarChart3 icon quando sem dados.

## File List
- apps/web/src/app/(authenticated)/dashboard/page.tsx
- apps/web/src/components/stats/revenue-chart.tsx (remover ou reescrever)
