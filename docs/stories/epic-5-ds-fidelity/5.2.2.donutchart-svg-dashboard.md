# Story 5.2.2: Substituir PaymentPieChart (Recharts) por DonutChart SVG do DS

## Status
Done

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
deploy_type: vercel

## Story
**As a** gestor do distribuidor,
**I want** ver formas de pagamento como DonutChart SVG do DS,
**so that** o visual siga o DS (stat central, legenda interativa, max 5 segmentos).

## Acceptance Criteria
1. Dashboard usa `DonutChart` de `@/components/ui/charts` no lugar de Recharts PieChart
2. Stat central mostra total ou porcentagem do segmento em hover
3. Legenda interativa: hover na legenda destaca segmento
4. Max 5 segmentos (agrupar "Outros" se necessario)
5. Cores via CHART_COLORS (laranja, orange-300, azul, verde, roxo)

## Tasks / Subtasks
- [x] Task 1: Adaptar dados de PaymentBreakdown para formato DonutChart
- [x] Task 2: Substituir componente PaymentPieChart por DonutChart do DS
- [x] Task 3: Implementar agrupamento "Outros" se > 5 metodos
- [x] Task 4: Testar interatividade (hover legenda/segmento)

## Effort
3 pontos

## Change Log
- 2026-06-12 @dev Done: Substituiu PaymentPieChart (Recharts) por DonutChart SVG do DS. Dados mapeados de PaymentBreakdown[] para {label, value, color} com CHART_COLORS e PAYMENT_METHOD_LABELS. Slice 5 segmentos max. centerValue=total BRL, centerLabel="Total", showPercent=false (mostra valor ao hover). Interatividade nativa do componente DonutChart.

## File List
- apps/web/src/app/(authenticated)/dashboard/page.tsx
- apps/web/src/components/stats/payment-pie-chart.tsx (remover ou reescrever)
