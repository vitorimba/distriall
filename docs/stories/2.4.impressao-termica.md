# Story 2.4: Impressao Termica Bluetooth

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "manual-review"]

## Story
**As a** Tiago,
**I want** imprimir cada pedido na impressora termica 58mm via Bluetooth (2 vias automaticas),
**so that** o entregador saia com os comprovantes fisicos sem precisar de outro sistema.

## Acceptance Criteria
1. Conectar com impressora termica 58mm via Bluetooth (Web Bluetooth API no Android Chrome)
2. Layout do cupom: nome do cliente, data, numero do pedido, lista de produtos (nome + qtd + valor), total, forma de pagamento, nome da conta
3. Impressao de 2 vias automatica (1a via = CLIENTE, 2a via = CONTROLE)
4. Botao "Imprimir" disponivel na tela de detalhes do pedido e na lista de pedidos (acao em lote)
5. Fallback se Bluetooth falhar: exibir cupom em tela formatado para screenshot/compartilhamento via WhatsApp
6. Configuracao de impressora em Settings com teste de impressao
7. Impressora "lembrada" apos primeiro pareamento (reconecta automaticamente)

## Tasks / Subtasks
- [ ] Task 1: Implementar ESCPOSBuilder (AC: #2)
  - [ ] 1.1 Criar `apps/web/src/lib/bluetooth/escpos.ts`
  - [ ] 1.2 Implementar metodos: `init()`, `align()`, `bold()`, `doubleSize()`, `text()`, `newline()`, `separator()`, `columns()`, `cut()`, `build()`
  - [ ] 1.3 Encoding: usar CP860 (Portuguese) para acentos, ou strip accents como fallback
  - [ ] 1.4 Largura de linha: 32 caracteres para 58mm
  - [ ] 1.5 Implementar conforme architecture doc secao 8.3

- [ ] Task 2: Implementar BluetoothPrinter connection manager (AC: #1, #7)
  - [ ] 2.1 Criar `apps/web/src/lib/bluetooth/printer.ts`
  - [ ] 2.2 Metodo `connect()`: usar `navigator.bluetooth.requestDevice()` com filtros de servico
  - [ ] 2.3 Metodo `print(data: Uint8Array)`: escrever no characteristic com chunking de 512 bytes
  - [ ] 2.4 Metodo `disconnect()`, getter `isConnected`
  - [ ] 2.5 Fallback de service UUID: tentar `000018f0-...` e `49535343-...` (impressoras genericas)
  - [ ] 2.6 Delay de 50ms entre chunks para evitar buffer overflow
  - [ ] 2.7 Implementar conforme architecture doc secao 8.4
  - [ ] 2.8 Salvar `device.id` no localStorage para reconexao automatica

- [ ] Task 3: Implementar receipt builder (AC: #2, #3)
  - [ ] 3.1 Criar `apps/web/src/lib/bluetooth/receipt.ts`
  - [ ] 3.2 Funcao `buildReceipt(order, items, accountName, via: 1 | 2)`: gera bytes ESC/POS
  - [ ] 3.3 Layout conforme architecture doc secao 8.5:
    - Header: "DISTRIALL" + nome da conta
    - Cliente + data + numero do pedido + forma pgto
    - Separador
    - Lista de produtos: nome truncado (20 chars) + qtd + valor
    - Separador
    - TOTAL em bold
    - Footer: "1a VIA - CLIENTE" ou "2a VIA - CONTROLE"
    - Cut
  - [ ] 3.4 Funcao `buildLoadingListReceipt(products, summary)` para lista de carregamento (Story 2.3)

- [ ] Task 4: Criar hook use-printer (AC: #1, #4, #7)
  - [ ] 4.1 Criar `apps/web/src/hooks/use-printer.ts`
  - [ ] 4.2 State: `isConnected`, `isConnecting`, `isPrinting`, `error`
  - [ ] 4.3 Actions: `connect()`, `disconnect()`, `printOrder(order, items, accountName)`, `printLoadingList(products, summary)`, `testPrint()`
  - [ ] 4.4 `printOrder` imprime automaticamente 2 vias (via 1 + via 2) com delay de 500ms entre elas
  - [ ] 4.5 `printBatch(orders[])`: imprime multiplos pedidos sequencialmente
  - [ ] 4.6 Auto-reconnect: ao chamar print, se desconectado tenta reconectar primeiro

- [ ] Task 5: Implementar botao de impressao no detalhe do pedido (AC: #4)
  - [ ] 5.1 Adicionar botao "Imprimir" em `apps/web/src/app/(authenticated)/orders/[id]/page.tsx`
  - [ ] 5.2 Ao clicar: conecta (se necessario) + imprime 2 vias
  - [ ] 5.3 Loading state no botao durante impressao
  - [ ] 5.4 Toast de sucesso: "Pedido impresso (2 vias)"
  - [ ] 5.5 Se falha: toast de erro + oferecer fallback (ver tela)

- [ ] Task 6: Implementar impressao em lote na lista de pedidos (AC: #4)
  - [ ] 6.1 Adicionar botao "Imprimir" na ActionBar de selecao em lote (Story 2.2)
  - [ ] 6.2 Imprime todos os pedidos selecionados sequencialmente (2 vias cada)
  - [ ] 6.3 Progress indicator: "Imprimindo pedido 3 de 5..."
  - [ ] 6.4 Se um pedido falha, continua com os proximos e reporta erros no final

- [ ] Task 7: Implementar fallback de visualizacao em tela (AC: #5)
  - [ ] 7.1 Criar `apps/web/src/components/orders/order-receipt.tsx`
  - [ ] 7.2 Renderizar cupom como HTML estilizado (similar ao layout termico)
  - [ ] 7.3 Fundo branco, fonte mono, layout identico ao cupom impresso
  - [ ] 7.4 Botao "Compartilhar" usando `navigator.share()` (Android)
  - [ ] 7.5 Botao "Copiar" para area de transferencia

- [ ] Task 8: Implementar tela de configuracao da impressora (AC: #6)
  - [ ] 8.1 Adicionar secao "Impressora Bluetooth" em `apps/web/src/app/(authenticated)/settings/page.tsx`
  - [ ] 8.2 Mostrar status: "Conectada: [nome]" ou "Nao configurada"
  - [ ] 8.3 Botao "Conectar Impressora": abre browser Bluetooth picker
  - [ ] 8.4 Botao "Testar Impressao": imprime cupom de teste com "TESTE DE IMPRESSAO - DISTRIALL"
  - [ ] 8.5 Botao "Desconectar"
  - [ ] 8.6 Instrucoes de troubleshooting inline

- [ ] Task 9: Verificar compatibilidade Web Bluetooth (AC: #1)
  - [ ] 9.1 Verificar `navigator.bluetooth` existe antes de mostrar opcoes de impressao
  - [ ] 9.2 Se nao suportado (iOS, Firefox): esconder botoes de Bluetooth, mostrar apenas fallback em tela
  - [ ] 9.3 Adicionar check na tela de Settings: "Web Bluetooth: Suportado / Nao suportado"

- [ ] Task 10: Testes (AC: all)
  - [ ] 10.1 Unit test: ESCPOSBuilder gera bytes corretos
  - [ ] 10.2 Unit test: buildReceipt gera layout esperado (comparar bytes)
  - [ ] 10.3 Unit test: truncate de nome de produto funciona
  - [ ] 10.4 Unit test: formatacao de moeda brasileira (R$ 1.234,56)
  - [ ] 10.5 Manual test: impressao real na impressora de Tiago (antes de deploy)

## Dev Notes

### Web Bluetooth API - Pontos Criticos

A Web Bluetooth API funciona APENAS em:
- Android Chrome 56+ (todos os usuarios alvo)
- Windows Chrome/Edge (Diego desktop)
- NAO funciona em iOS Safari (irrelevante - nenhum usuario usa iOS)

**Requisitos:**
- Pagina servida via HTTPS (Vercel ja garante)
- Primeiro pareamento exige gesto do usuario (tap no botao)
- Device picker nativo do browser aparece automaticamente

### Service UUIDs Conhecidos (Impressoras Termicas)

```typescript
// Impressoras genericas 58mm BLE
const KNOWN_SERVICES = [
  '000018f0-0000-1000-8000-00805f9b34fb', // Servico padrao ESC/POS
  '49535343-fe7d-4ae5-8fa9-9fafd205e455', // Nordic UART (muitas impressoras chinesas)
];

const KNOWN_CHARACTERISTICS = [
  '00002af1-0000-1000-8000-00805f9b34fb', // Write characteristic padrao
  '49535343-8841-43f4-a8d4-ecbe34729bb3', // Nordic UART TX
];
```

### ESC/POS Builder (Architecture Doc Secao 8.3)

Implementar exatamente conforme o architecture doc. Comandos-chave:
- `0x1B 0x40` = ESC @ (init)
- `0x1B 0x61 n` = ESC a (align: 0=left, 1=center, 2=right)
- `0x1B 0x45 n` = ESC E (bold: 0=off, 1=on)
- `0x1D 0x21 n` = GS ! (double size: 0x11=on, 0x00=off)
- `0x0A` = LF (newline)
- `0x1D 0x56 0x00` = GS V (full cut)

Largura de impressao 58mm = 32 caracteres por linha em fonte normal.

### Receipt Layout (Architecture Doc Secao 8.5)

```
================================  <- 32 chars
        DISTRIALL
   Distrial Rio Preto
================================
Cliente: Frigorifico Espanha
Data: 08/06/2026  Pedido: #1234
Pgto: Boleto
--------------------------------
Produto              Qtd  Valor
--------------------------------
Tempero caseiro 500g  67  259,50
Alho descascado       17  330,00
Mezabom 500gr          9  144,00
Creme cebola kg        8  144,00
Ervas mistas 250gr     8  116,00
--------------------------------
TOTAL:              R$ 993,50
================================
1a VIA - CLIENTE
================================
```

### BLE Chunking

BLE tem limite de ~512 bytes por write. O print manager deve:
1. Dividir o buffer em chunks de 512 bytes
2. Enviar cada chunk com `writeValueWithResponse`
3. Delay de 50ms entre chunks para nao estourar buffer da impressora

```typescript
const CHUNK_SIZE = 512;
for (let i = 0; i < data.length; i += CHUNK_SIZE) {
  const chunk = data.slice(i, i + CHUNK_SIZE);
  await characteristic.writeValueWithResponse(chunk);
  await new Promise(resolve => setTimeout(resolve, 50));
}
```

### Fluxo de Impressao (UX)

1. **Setup (uma vez):** Settings > Impressora Bluetooth > Conectar > Browser picker > Selecionar impressora
2. **Pedido individual:** Detalhe pedido > "Imprimir" > Auto-reconnect se necessario > 2 vias impressas
3. **Lote:** Lista pedidos > Selecionar > ActionBar "Imprimir" > Imprime todos sequencialmente
4. **Carregamento:** BottomSheet > "Imprimir Lista" > Imprime lista consolidada (1 via)

### Fallback HTML Receipt

Quando Bluetooth nao esta disponivel, renderizar como componente React:
- Container com `max-width: 300px`, `font-family: monospace`, `font-size: 12px`
- Fundo branco, borda fina
- Mesmo layout do cupom termico
- Botoes: "Compartilhar" (`navigator.share()`) e "Copiar"

### Folder Structure

```
apps/web/src/
  lib/
    bluetooth/
      escpos.ts         -- ESC/POS command builder
      printer.ts        -- Bluetooth connection manager
      receipt.ts        -- Receipt layout builders (order + loading list)
  hooks/
    use-printer.ts      -- React hook for printing
  components/
    orders/
      order-receipt.tsx  -- HTML fallback receipt
  app/
    (authenticated)/
      settings/
        page.tsx         -- Secao de impressora (adicionar)
```

### Encoding de Acentos

Impressoras ESC/POS usam code pages, nao UTF-8. Opcoes:
1. **CP860** (Portuguese): suporta acentos pt-BR nativamente. Comando: `0x1B 0x74 0x03`
2. **Strip accents**: remover acentos (ex: "Opcao" -> "Opcao"). Mais simples, menos elegante.

Recomendacao: tentar CP860 primeiro; se impressora nao suportar, usar strip accents como fallback.

### Known Limitations (Architecture Doc Secao 8.9)

- Impressoras Bluetooth Classic (nao BLE) NAO funcionam com Web Bluetooth
- Primeiro pareamento requer gesto do usuario (seguranca do browser)
- Sem impressao em background (app deve estar aberto)
- Alguns modelos chineses baratos usam UUIDs nao-padrao (fallback com `acceptAllDevices`)

### Testing

- **Unit tests** (`tests/unit/escpos.test.ts`):
  - ESCPOSBuilder.init() gera `[0x1B, 0x40]`
  - ESCPOSBuilder.align('center') gera `[0x1B, 0x61, 0x01]`
  - ESCPOSBuilder.bold(true) gera `[0x1B, 0x45, 0x01]`
  - ESCPOSBuilder.columns('Item', 'R$ 10,00', 32) preenche com espacos
  - ESCPOSBuilder.cut() gera `[0x0A, 0x0A, 0x0A, 0x1D, 0x56, 0x00]`
- **Unit tests** (`tests/unit/receipt.test.ts`):
  - buildReceipt gera buffer com header DISTRIALL
  - buildReceipt via 1 contem "1a VIA - CLIENTE"
  - buildReceipt via 2 contem "2a VIA - CONTROLE"
  - Nomes de produto truncados a 20 chars
  - Valores formatados em BRL
- **Manual testing** (OBRIGATORIO antes de deploy):
  - Testar impressao real no celular e impressora do Tiago
  - Verificar alinhamento de colunas
  - Verificar acentos (ou fallback strip accents)
  - Testar 2 vias sequenciais
  - Testar reconexao apos desligar/religar impressora

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-08 | 1.0 | Story created from PRD | River (SM) |
