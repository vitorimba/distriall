# Story 1.1: Setup do Projeto e Infraestrutura Base

## Status
Ready

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "manual-review"]

## Story
**As a** desenvolvedor,
**I want** ter o projeto inicializado com stack definida, banco de dados configurado e deploy automatico,
**so that** toda feature subsequente tenha uma base solida para ser implementada.

## Acceptance Criteria
1. Repositorio configurado como monorepo com pnpm workspaces (packages/shared + apps/web)
2. Projeto Next.js 15.x (App Router) inicializado com PWA configurada via next-pwa/Serwist
3. Supabase projeto criado com PostgreSQL acessivel (local dev via `supabase init` + cloud project)
4. Deploy automatico configurado (push para main → deploy Vercel)
5. Pagina de health-check acessivel em producao (rota `/` retorna status OK)
6. TypeScript configurado em modo strict em todo o monorepo
7. Tailwind CSS 4.x + shadcn/ui inicializados com tema base
8. Zustand instalado e configurado
9. Estrutura de pastas conforme arquitetura definida

## Tasks / Subtasks
- [x] Task 1: Inicializar monorepo com pnpm workspaces (AC: #1, #6)
  - [x] 1.1 Criar `package.json` raiz com `"workspaces"` config
  - [x] 1.2 Criar `pnpm-workspace.yaml` com entries: `packages/*` e `apps/*`
  - [x] 1.3 Criar `packages/shared/` com `package.json`, `tsconfig.json` e `src/index.ts`
  - [x] 1.4 Criar `packages/shared/src/types/enums.ts` com enums de status, roles e payment methods
  - [x] 1.5 Criar `packages/shared/src/types/models.ts` com tipos de dominio base (placeholder)
  - [x] 1.6 Criar `packages/shared/src/utils/currency.ts` com formatacao BRL
  - [x] 1.7 Configurar `tsconfig.json` raiz com `strict: true`, path aliases
- [x] Task 2: Inicializar Next.js 15.x com App Router (AC: #2, #7)
  - [x] 2.1 Rodar `pnpm create next-app apps/web --typescript --tailwind --app --src-dir`
  - [x] 2.2 Configurar `next.config.js` para PWA com next-pwa/Serwist
  - [x] 2.3 Criar `apps/web/public/manifest.json` com nome "Distriall", icons, theme_color
  - [x] 2.4 Criar `apps/web/public/offline.html` pagina fallback offline
  - [x] 2.5 Instalar e configurar shadcn/ui (`npx shadcn-ui@latest init`)
  - [x] 2.6 Configurar tema base Tailwind (cores verde/branco similar ao Kite, alto contraste)
  - [x] 2.7 Instalar Zustand 5.x
  - [x] 2.8 Criar pagina de health-check em `apps/web/src/app/page.tsx` (exibir status OK)
- [x] Task 3: Configurar Supabase local + cloud (AC: #3)
  - [x] 3.1 Rodar `supabase init` na raiz, criar pasta `supabase/`
  - [x] 3.2 Configurar `supabase/config.toml` com project settings
  - [ ] 3.3 Criar projeto Supabase Cloud (via dashboard ou CLI) — **ACAO DO USUARIO**
  - [x] 3.4 Criar `apps/web/.env.local.example` com `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - [x] 3.5 Instalar `@supabase/supabase-js` e `@supabase/ssr` no apps/web
  - [x] 3.6 Criar `apps/web/src/lib/supabase/client.ts` (browser client)
  - [x] 3.7 Criar `apps/web/src/lib/supabase/server.ts` (server client)
  - [x] 3.8 Criar `apps/web/src/lib/supabase/middleware.ts` (auth middleware)
- [x] Task 4: Estrutura de pastas completa (AC: #9)
  - [x] 4.1 Criar arvore de diretorios em `apps/web/src/` conforme arquitetura:
    - `app/(authenticated)/` com layout.tsx placeholder
    - `app/(authenticated)/dashboard/page.tsx` placeholder
    - `app/(authenticated)/orders/page.tsx` placeholder
    - `app/(authenticated)/products/page.tsx` placeholder
    - `app/(authenticated)/clients/page.tsx` placeholder
    - `app/(authenticated)/financial/` placeholders
    - `app/(authenticated)/stats/page.tsx` placeholder
    - `app/(authenticated)/settings/page.tsx` placeholder
    - `app/driver/` com layout.tsx e page.tsx placeholder
    - `app/login/page.tsx` placeholder
  - [x] 4.2 Criar pastas de componentes: `components/ui/`, `components/layout/`, `components/orders/`, `components/products/`, `components/clients/`, `components/financial/`, `components/stats/`
  - [x] 4.3 Criar pastas de lib: `lib/bluetooth/`, `lib/offline/`, `lib/utils/`
  - [x] 4.4 Criar pastas: `hooks/`, `stores/`, `providers/`
- [ ] Task 5: Deploy automatico Vercel (AC: #4, #5) — **ACAO DO USUARIO**
  - [ ] 5.1 Conectar repositorio ao Vercel
  - [ ] 5.2 Configurar root directory como `apps/web` no Vercel
  - [ ] 5.3 Configurar environment variables no Vercel (SUPABASE_URL, SUPABASE_ANON_KEY)
  - [ ] 5.4 Verificar deploy automatico no push
  - [ ] 5.5 Acessar URL de producao e confirmar health-check
- [x] Task 6: CI basico com GitHub Actions (AC: #4)
  - [x] 6.1 Criar `.github/workflows/ci.yml` com jobs: lint, typecheck, test
  - [x] 6.2 Configurar cache de pnpm no CI
  - [x] 6.3 Rodar `pnpm lint`, `pnpm typecheck` no CI

## Dev Notes

### Stack Definitiva
| Camada | Tecnologia | Versao |
|--------|-----------|--------|
| Frontend | Next.js (App Router) | 15.x |
| PWA | next-pwa (Serwist) | latest |
| UI | Tailwind CSS + shadcn/ui | 4.x / latest |
| State | Zustand | 5.x |
| Backend | Supabase (BaaS) | latest |
| DB | PostgreSQL (via Supabase) | 15+ |
| Auth | Supabase Auth | - |
| Offline DB | Dexie.js (IndexedDB) | 4.x |
| Package Manager | pnpm | 9.x |
| Language | TypeScript | 5.x |

### Estrutura de Pastas Completa
```
distriall/
├── .github/workflows/ci.yml
├── packages/
│   └── shared/
│       ├── src/
│       │   ├── types/
│       │   │   ├── database.ts       # Generated Supabase types (gerado depois)
│       │   │   ├── models.ts         # Domain models
│       │   │   └── enums.ts          # Status, roles, payment methods
│       │   ├── utils/
│       │   │   ├── currency.ts       # formatBRL(value: number): string
│       │   │   ├── calculations.ts   # Profit, settlement calculations
│       │   │   └── validation.ts     # Zod schemas
│       │   └── index.ts
│       ├── package.json
│       └── tsconfig.json
├── apps/
│   └── web/                          # Next.js PWA
│       ├── public/
│       │   ├── manifest.json
│       │   ├── icons/                # 192x192, 512x512
│       │   └── offline.html
│       ├── src/
│       │   ├── app/
│       │   │   ├── layout.tsx        # Root layout com providers
│       │   │   ├── page.tsx          # Health-check / redirect
│       │   │   ├── login/page.tsx
│       │   │   ├── (authenticated)/layout.tsx
│       │   │   ├── (authenticated)/dashboard/page.tsx
│       │   │   ├── (authenticated)/orders/page.tsx
│       │   │   ├── (authenticated)/products/page.tsx
│       │   │   ├── (authenticated)/clients/page.tsx
│       │   │   ├── (authenticated)/financial/
│       │   │   ├── (authenticated)/stats/page.tsx
│       │   │   ├── (authenticated)/settings/page.tsx
│       │   │   └── driver/layout.tsx + page.tsx
│       │   ├── components/
│       │   │   ├── ui/               # shadcn/ui
│       │   │   ├── layout/           # header, sidebar, bottom-nav
│       │   │   ├── orders/
│       │   │   ├── products/
│       │   │   ├── clients/
│       │   │   ├── financial/
│       │   │   └── stats/
│       │   ├── lib/
│       │   │   ├── supabase/client.ts, server.ts, middleware.ts
│       │   │   ├── bluetooth/
│       │   │   ├── offline/
│       │   │   └── utils/format.ts, constants.ts
│       │   ├── hooks/
│       │   ├── stores/
│       │   └── providers/
│       ├── next.config.js
│       ├── tailwind.config.ts
│       └── package.json
├── supabase/
│   ├── config.toml
│   ├── migrations/
│   └── seed.sql
├── tests/
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── package.json                      # Root workspace
├── pnpm-workspace.yaml
└── turbo.json                        # Opcional
```

### pnpm-workspace.yaml
```yaml
packages:
  - "packages/*"
  - "apps/*"
```

### Root package.json scripts
```json
{
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "typecheck": "pnpm --filter web tsc --noEmit",
    "test": "pnpm --filter shared test"
  }
}
```

### PWA manifest.json
```json
{
  "name": "Distriall",
  "short_name": "Distriall",
  "description": "Gestao de vendas e distribuicao",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#16a34a",
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

### next.config.js com PWA
```javascript
const withPWA = require('next-pwa')({
  dest: 'public',
  register: true,
  skipWaiting: true,
  disable: process.env.NODE_ENV === 'development',
});

/** @type {import('next').NextConfig} */
const nextConfig = {
  // nada especial por enquanto
};

module.exports = withPWA(nextConfig);
```

### Supabase Client (browser)
```typescript
// apps/web/src/lib/supabase/client.ts
import { createBrowserClient } from '@supabase/ssr';

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
}
```

### Supabase Client (server)
```typescript
// apps/web/src/lib/supabase/server.ts
import { createServerClient } from '@supabase/ssr';
import { cookies } from 'next/headers';

export async function createClient() {
  const cookieStore = await cookies();
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return cookieStore.getAll(); },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value, options }) => {
            cookieStore.set(name, value, options);
          });
        },
      },
    }
  );
}
```

### .env.local.example
```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

### Enums iniciais (packages/shared/src/types/enums.ts)
```typescript
export const UserRole = {
  ADMIN: 'admin',
  VENDEDOR: 'vendedor',
  ENTREGADOR: 'entregador',
} as const;
export type UserRole = typeof UserRole[keyof typeof UserRole];

export const AccountUserRole = {
  ADMIN: 'admin',
  VENDEDOR: 'vendedor',
  ENTREGADOR: 'entregador',
  VISUALIZADOR: 'visualizador',
} as const;
export type AccountUserRole = typeof AccountUserRole[keyof typeof AccountUserRole];

export const OrderStatus = {
  LANCADO: 'lancado',
  CONFIRMADO: 'confirmado',
  CARREGADO: 'carregado',
  ENTREGUE: 'entregue',
  CANCELADO: 'cancelado',
} as const;
export type OrderStatus = typeof OrderStatus[keyof typeof OrderStatus];

export const PaymentMethod = {
  DINHEIRO: 'dinheiro',
  PIX: 'pix',
  BOLETO: 'boleto',
  VALE: 'vale',
  CARTAO: 'cartao',
  MISTO: 'misto',
} as const;
export type PaymentMethod = typeof PaymentMethod[keyof typeof PaymentMethod];
```

### GitHub Actions CI (.github/workflows/ci.yml)
```yaml
name: CI
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
```

### Testing
- Verificar que `pnpm install` roda sem erros no monorepo
- Verificar que `pnpm dev` sobe o Next.js em localhost:3000
- Verificar que a pagina de health-check renderiza corretamente
- Verificar que `pnpm build` compila sem erros TypeScript
- Verificar que `pnpm lint` passa
- Verificar que o PWA manifest e carregado (DevTools > Application > Manifest)
- Verificar que o Service Worker registra (DevTools > Application > Service Workers)
- Verificar que `supabase start` sobe o banco local
- Verificar deploy no Vercel apos push

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### Debug Log References
N/A

### Completion Notes List
- Next.js 16.2.7 instalado (versao mais recente, story pedia 15.x — upgrade compativel)
- Tailwind CSS 4.3.0 instalado via create-next-app
- shadcn/ui 4.11.0 inicializado com componente Button e utils
- Zustand 5.x instalado
- Supabase SSR + client JS instalados
- next-pwa configurado com Turbopack compatibility (`turbopack: {}`)
- Tema primario customizado para verde (#16a34a / oklch green)
- Tipo `themeColor` movido para `viewport` export (Next.js 16 requirement)
- Declaracao de tipo para `next-pwa` criada (sem @types disponivel)
- Task 3.3 (Supabase Cloud) e Task 5 (Vercel) requerem acao manual do usuario
- `pnpm build` passa sem erros
- `pnpm lint` passa sem erros

### File List
| File | Action | Description |
|------|--------|-------------|
| `package.json` | Created | Root workspace package.json |
| `pnpm-workspace.yaml` | Created | pnpm workspace config |
| `tsconfig.json` | Created | Root TypeScript strict config |
| `.gitignore` | Modified | Added .next, PWA, pnpm entries |
| `.github/workflows/ci.yml` | Created | CI with lint + typecheck |
| `packages/shared/package.json` | Created | Shared package config |
| `packages/shared/tsconfig.json` | Created | Shared TypeScript config |
| `packages/shared/src/index.ts` | Created | Barrel exports |
| `packages/shared/src/types/enums.ts` | Created | Status, roles, payment enums |
| `packages/shared/src/types/models.ts` | Created | Domain model types (placeholder) |
| `packages/shared/src/utils/currency.ts` | Created | formatBRL utility |
| `apps/web/` | Created | Next.js 16 PWA app (via create-next-app) |
| `apps/web/next.config.ts` | Modified | PWA + Turbopack config |
| `apps/web/src/app/layout.tsx` | Modified | Distriall metadata, pt-BR, viewport |
| `apps/web/src/app/page.tsx` | Modified | Health-check page (Status: OK) |
| `apps/web/src/app/globals.css` | Modified | Green primary theme |
| `apps/web/src/types/next-pwa.d.ts` | Created | Type declaration for next-pwa |
| `apps/web/public/manifest.json` | Created | PWA manifest |
| `apps/web/public/offline.html` | Created | Offline fallback page |
| `apps/web/.env.local.example` | Created | Environment variables template |
| `apps/web/src/lib/supabase/client.ts` | Created | Browser Supabase client |
| `apps/web/src/lib/supabase/server.ts` | Created | Server Supabase client |
| `apps/web/src/lib/supabase/middleware.ts` | Created | Auth middleware |
| `apps/web/src/app/(authenticated)/layout.tsx` | Created | Auth layout placeholder |
| `apps/web/src/app/(authenticated)/dashboard/page.tsx` | Created | Dashboard placeholder |
| `apps/web/src/app/(authenticated)/orders/page.tsx` | Created | Orders placeholder |
| `apps/web/src/app/(authenticated)/products/page.tsx` | Created | Products placeholder |
| `apps/web/src/app/(authenticated)/clients/page.tsx` | Created | Clients placeholder |
| `apps/web/src/app/(authenticated)/financial/page.tsx` | Created | Financial placeholder |
| `apps/web/src/app/(authenticated)/stats/page.tsx` | Created | Stats placeholder |
| `apps/web/src/app/(authenticated)/settings/page.tsx` | Created | Settings placeholder |
| `apps/web/src/app/driver/layout.tsx` | Created | Driver layout placeholder |
| `apps/web/src/app/driver/page.tsx` | Created | Driver page placeholder |
| `apps/web/src/app/login/page.tsx` | Created | Login placeholder |
| `supabase/config.toml` | Created | Supabase local config |
| `supabase/seed.sql` | Created | Seed data placeholder |

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-08 | 1.0 | Story created from PRD | River (SM) |
| 2026-06-08 | 1.1 | Story validated (GO 8/10) — Status Draft → Ready | Pax (PO) |
| 2026-06-08 | 1.2 | Tasks 1-4, 6 implemented. Tasks 3.3, 5 require user action. Build+lint pass. | Dex (Dev) |
