# Story 3.3: Registro de Gastos por Conta

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "manual-review"]

## Story
**As a** Tiago (admin),
**I want** registrar gastos operacionais (impostos, gasolina, manutencao) no app por conta, incluindo gastos compartilhados com rateio entre contas,
**so that** eles entrem automaticamente no calculo do acerto semanal e eu nao precise mais usar bloco de notas.

## Acceptance Criteria
1. CRUD de gastos com campos: descricao, valor, data, conta, categoria (fixo/variavel), tipo (impostos, gasolina, manutencao, outros), recorrencia (unico/semanal/mensal)
2. Gastos fixos pre-cadastrados (ex: impostos R$320/semana para Distrial) que aparecem automaticamente no acerto
3. Gastos compartilhados: flag `compartilhado` com percentual de rateio configuravel por conta (tabela `expense_splits`)
4. Ex: gasolina R$500 — 50% Distrial RP (R$250), 50% Tiago (R$250) — percentuais configuraveis
5. Lista de gastos com filtro por conta, periodo e categoria
6. Soma total de gastos por periodo visivel na lista
7. Apenas admin e usuarios com `can_view_financial = true` podem ver/gerenciar gastos (RLS)

## Tasks / Subtasks
- [ ] Task 1: Criar tabelas `expenses` e `expense_splits` com migrations (AC: #1, #3, #7)
  - [ ] 1.1 Criar migration `supabase/migrations/XXXXX_create_expenses.sql`
  - [ ] 1.2 Criar migration para `expense_splits` (na mesma ou migration separada)
  - [ ] 1.3 Adicionar RLS policies para ambas as tabelas (financial visibility required)
- [ ] Task 2: Criar tipos TypeScript (AC: #1, #3)
  - [ ] 2.1 Adicionar tipos em `packages/shared/src/types/models.ts` — `Expense`, `ExpenseSplit`
  - [ ] 2.2 Adicionar enums em `packages/shared/src/types/enums.ts`: `ExpenseCategory`, `ExpenseType`, `ExpenseRecurrence`
  - [ ] 2.3 Adicionar validacao Zod em `packages/shared/src/utils/validation.ts`
- [ ] Task 3: Implementar formulario de gasto (AC: #1, #2, #3, #4)
  - [ ] 3.1 Criar componente `apps/web/src/components/financial/expense-form.tsx`
  - [ ] 3.2 Campos: descricao, valor, data, categoria (fixo/variavel), tipo (dropdown), recorrencia
  - [ ] 3.3 Toggle "Gasto compartilhado" — ao ativar, exibir lista de contas com campo percentual para cada
  - [ ] 3.4 Validar que percentuais somam 100%
  - [ ] 3.5 Calcular automaticamente o valor por conta (valor * percentual / 100)
- [ ] Task 4: CRUD de expenses via Supabase (AC: #1, #2, #3)
  - [ ] 4.1 Criar hook `apps/web/src/hooks/use-expenses.ts`
  - [ ] 4.2 Ao salvar gasto compartilhado, criar registros em `expense_splits` para cada conta
  - [ ] 4.3 Ao editar gasto, atualizar splits correspondentes
  - [ ] 4.4 Ao deletar gasto, cascade deleta splits (FK ON DELETE CASCADE)
- [ ] Task 5: Pagina de lista de gastos (AC: #5, #6)
  - [ ] 5.1 Criar pagina `apps/web/src/app/(authenticated)/financial/expenses/page.tsx`
  - [ ] 5.2 Lista com colunas: descricao, valor, data, categoria, tipo, compartilhado (badge), recorrencia
  - [ ] 5.3 Filtros: periodo (date range), categoria (fixo/variavel), tipo
  - [ ] 5.4 Card no topo com soma total dos gastos filtrados
  - [ ] 5.5 Para gastos compartilhados, exibir tooltip com rateio por conta
- [ ] Task 6: Pre-cadastro de gastos fixos recorrentes (AC: #2)
  - [ ] 6.1 Criar seed data com gastos fixos exemplo (impostos R$320/semana)
  - [ ] 6.2 Logica para gerar automaticamente gastos recorrentes (semanal/mensal) — pode ser Edge Function ou logica no acerto
- [ ] Task 7: Testes (AC: todos)
  - [ ] 7.1 Unit test: calculo de rateio (valor * percentual / 100)
  - [ ] 7.2 Unit test: validacao de percentuais somam 100%
  - [ ] 7.3 Integration test: criar gasto compartilhado, verificar splits, verificar soma

## Dev Notes

### Database Schema

Tabela `expenses`:

```sql
CREATE TABLE public.expenses (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  account_id UUID NOT NULL REFERENCES public.accounts(id) ON DELETE CASCADE,
  description TEXT NOT NULL,
  amount NUMERIC(12,2) NOT NULL,
  category TEXT NOT NULL CHECK (category IN ('fixo', 'variavel')),
  type TEXT, -- 'impostos', 'gasolina', 'manutencao', 'outros'
  recurrence TEXT CHECK (recurrence IN ('unico', 'semanal', 'mensal')),
  is_shared BOOLEAN NOT NULL DEFAULT false,
  expense_date DATE NOT NULL,
  created_by UUID NOT NULL REFERENCES public.users(id),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_expenses_account ON public.expenses(account_id);
CREATE INDEX idx_expenses_date ON public.expenses(expense_date);
CREATE INDEX idx_expenses_category ON public.expenses(account_id, category);
CREATE INDEX idx_expenses_shared ON public.expenses(is_shared) WHERE is_shared = true;
```

Tabela `expense_splits`:

```sql
CREATE TABLE public.expense_splits (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  expense_id UUID NOT NULL REFERENCES public.expenses(id) ON DELETE CASCADE,
  account_id UUID NOT NULL REFERENCES public.accounts(id) ON DELETE CASCADE,
  percentage NUMERIC(5,2) NOT NULL CHECK (percentage > 0 AND percentage <= 100),
  amount NUMERIC(12,2) NOT NULL, -- calculated: expense.amount * (percentage / 100)
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),

  UNIQUE(expense_id, account_id)
);

CREATE INDEX idx_expense_splits_expense ON public.expense_splits(expense_id);
CREATE INDEX idx_expense_splits_account ON public.expense_splits(account_id);
```

### RLS Policies

```sql
-- expenses: financial visibility required
CREATE POLICY "Financial visibility required for expenses"
  ON public.expenses FOR SELECT
  USING (
    account_id = ANY(get_user_account_ids())
    AND can_view_financial(account_id)
  );

CREATE POLICY "Admins can create expenses"
  ON public.expenses FOR INSERT
  WITH CHECK (
    account_id = ANY(get_user_account_ids())
    AND get_user_role_in_account(account_id) = 'admin'
  );

CREATE POLICY "Admins can update expenses"
  ON public.expenses FOR UPDATE
  USING (account_id = ANY(get_user_account_ids()))
  WITH CHECK (get_user_role_in_account(account_id) = 'admin');

CREATE POLICY "Admins can delete expenses"
  ON public.expenses FOR DELETE
  USING (
    account_id = ANY(get_user_account_ids())
    AND get_user_role_in_account(account_id) = 'admin'
  );

-- expense_splits: same financial visibility
CREATE POLICY "Financial visibility for expense splits"
  ON public.expense_splits FOR SELECT
  USING (account_id = ANY(get_user_account_ids()) AND can_view_financial(account_id));

CREATE POLICY "Admins can manage expense splits"
  ON public.expense_splits FOR INSERT
  WITH CHECK (account_id = ANY(get_user_account_ids()) AND get_user_role_in_account(account_id) = 'admin');
```

### Folder Structure

```
apps/web/src/
  components/
    financial/
      expense-form.tsx          <-- NEW: formulario de gasto com rateio
      expense-list.tsx          <-- NEW: lista filtravel
      expense-split-editor.tsx  <-- NEW: editor de percentuais por conta
  hooks/
    use-expenses.ts             <-- NEW: CRUD hook for expenses + splits
  app/(authenticated)/
    financial/
      expenses/
        page.tsx                <-- NEW: pagina de gastos
packages/shared/src/
  types/
    enums.ts                    <-- MODIFY: add ExpenseCategory, ExpenseType, ExpenseRecurrence
    models.ts                   <-- MODIFY: add Expense, ExpenseSplit types
  utils/
    calculations.ts             <-- MODIFY: add split calculation function
    validation.ts               <-- MODIFY: add expense validation schema
supabase/migrations/
  XXXXX_create_expenses.sql     <-- NEW
```

### Logica de Negocio

- **Gasto simples (nao compartilhado):** 1 registro em `expenses` com `is_shared = false`. Nenhum registro em `expense_splits`.
- **Gasto compartilhado:** 1 registro em `expenses` com `is_shared = true`, + N registros em `expense_splits` (1 por conta participante). O `account_id` do expense e a conta "dona" (quem registrou). Os splits definem quanto cada conta paga.
- **Rateio:** Percentuais configuraveis. Exemplo: gasolina R$500, splits = [{account: 'Distrial RP', percentage: 50, amount: 250}, {account: 'Tiago', percentage: 50, amount: 250}]. Validacao: `SUM(percentage) = 100`.
- **Calculo do amount no split:** `expense.amount * (percentage / 100)`. Arredondar para 2 casas decimais. Ajustar o ultimo split para absorver diferenca de centavos.
- **Recorrencia:** Para v1, gastos recorrentes sao criados manualmente. O campo `recurrence` serve como metadata para o acerto semanal saber quais gastos esperar. Automacao futura via Supabase Edge Function ou pg_cron.
- **Acesso:** Apenas admin pode criar/editar/deletar gastos. Usuarios com `can_view_financial = true` podem visualizar.

### Calculo de Rateio (utility function)

```typescript
// packages/shared/src/utils/calculations.ts
export function calculateSplits(
  totalAmount: number,
  splits: { accountId: string; percentage: number }[]
): { accountId: string; percentage: number; amount: number }[] {
  const result = splits.map((s, i) => ({
    accountId: s.accountId,
    percentage: s.percentage,
    amount: Math.round((totalAmount * s.percentage / 100) * 100) / 100,
  }));

  // Adjust last split for rounding difference
  const sumAmounts = result.reduce((sum, s) => sum + s.amount, 0);
  const diff = Math.round((totalAmount - sumAmounts) * 100) / 100;
  if (diff !== 0 && result.length > 0) {
    result[result.length - 1].amount += diff;
  }

  return result;
}
```

### Dependencias

- Nenhuma dependencia de outras stories do Epic 3. Pode ser implementada em paralelo com Story 3.1.

### Testing

- **Unit tests** em `tests/unit/`:
  - `expense-splits.test.ts`: calculo de rateio com diferentes percentuais, arredondamento correto, soma = total
  - `expense-validation.test.ts`: validacao de campos obrigatorios, percentuais somam 100%
- **Integration tests** em `tests/integration/`:
  - `expense-flow.test.ts`: criar gasto simples, criar gasto compartilhado com splits, editar splits, deletar

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-08 | 1.0 | Story created from PRD | River (SM) |
