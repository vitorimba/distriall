# Story 1.4: Cadastro de Produtos com Variacoes

## Status
Done

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "manual-review"]

## Story
**As a** Tiago,
**I want** cadastrar meus ~200 produtos com nome, peso, variacoes de tamanho, preco de custo e preco de venda,
**so that** eu tenha meu catalogo completo no sistema.

## Scope

### IN
- Tabelas `products` e `product_variants` com RLS multi-tenant
- CRUD completo de produtos + variacoes
- Lista com busca e filtro por categoria
- Formulario de criacao/edicao com variacoes inline
- Edicao de precos em lote (valor fixo ou percentual)
- Soft delete (is_active=false)
- Validacao Zod

### OUT
- Preco diferenciado por cliente (Story 1.5 — `client_prices`)
- Import/export de catalogo em massa
- Imagens de produto
- Categorias como entidade separada (usa campo texto livre)

## Dependencies
- **Story 1.3** (Multi-Conta) — COMPLETED. accounts, account_users, RLS functions, account store/provider/switcher

## Complexity
- **Estimate:** L (Large)
- **Points:** 8
- **Rationale:** 2 tabelas + RLS + 3 páginas frontend + formulário complexo com variacoes dinâmicas + batch editor. Primeira feature CRUD real do app.

## Risks
- **R1:** Formulário de variacoes dinâmicas é complexo em React. **Mitigacao:** useFieldArray pattern ou array state simples.
- **R2:** Batch price update pode falhar parcialmente. **Mitigacao:** Usar transação ou aceitar partial success com feedback.

## Acceptance Criteria
1. CRUD completo de produtos vinculado a conta ativa (apenas admin e vendedor podem criar/editar)
2. Campos do produto: nome, descricao (opcional), categoria (opcional), unidade (un, kg, g, l)
3. Suporte a variacoes de tamanho no mesmo produto (ex: Creme de Cebola 500g, 1kg, 25kg como variacoes) — tabela `product_variants`
4. Cada variacao tem: nome (ex: "500g"), peso em gramas, preco de custo (NUMERIC 10,2), preco de venda (NUMERIC 10,2), SKU (opcional)
5. Lista de produtos com busca por nome e filtro por categoria
6. Edicao de precos em lote: selecionar N produtos/variacoes, atualizar preco de venda e/ou custo
7. Cada conta tem seu proprio catalogo com precos independentes (isolamento por account_id + RLS)
8. Soft delete: produto/variacao desativado (is_active=false) nao aparece em novas operacoes mas mantem historico

## Tasks / Subtasks
- [x] Task 1: Criar migration das tabelas `products` e `product_variants` (AC: #1, #3, #4, #7)
  - [x] 1.1 Criar `supabase/migrations/00005_products.sql` com tabela `products`
  - [x] 1.2 Criar tabela `product_variants` na mesma migration
  - [x] 1.3 Criar indexes + updated_at triggers
  - [x] 1.4 Aplicar migration via `supabase db push`
- [x] Task 2: Configurar RLS para products e product_variants (AC: #7)
  - [x] 2.1 Criar `supabase/migrations/00006_rls_products.sql`
  - [x] 2.2 Habilitar RLS em `products` e `product_variants`
  - [x] 2.3 Policy SELECT: usuario ve produtos das contas que pertence
  - [x] 2.4 Policy INSERT/UPDATE: admin ou vendedor da conta
  - [x] 2.5 Policy DELETE: apenas admin da conta
- [x] Task 3: Pagina de listagem de produtos (AC: #5)
  - [x] 3.1 Editar `apps/web/src/app/(authenticated)/products/page.tsx`
  - [x] 3.2 Listar produtos da conta ativa com variacoes (select + join)
  - [x] 3.3 Busca por nome via `ilike`
  - [x] 3.4 Filtro por categoria (dropdown dinamico)
  - [x] 3.5 Nome, qtd variacoes, faixa de preco (min-max)
  - [x] 3.6 Expandir produto para ver variacoes com precos
  - [x] 3.7 FAB "+" flutuante para novo produto
  - [x] 3.8 Mobile-first com cards
- [x] Task 4: Formulario de criacao/edicao de produto (AC: #1, #2, #3, #4, #8)
  - [x] 4.1 Criar `products/new/page.tsx`
  - [x] 4.2 Criar `products/[id]/page.tsx`
  - [x] 4.3 Criar `product-form.tsx` reutilizavel
  - [x] 4.4 Campos: nome, descricao, categoria, unidade
  - [x] 4.5 Variacoes inline: add/remove, nome, peso, custo, venda
  - [x] 4.6 Create: INSERT product + INSERT variants
  - [x] 4.7 Edit: UPDATE product + upsert variants
  - [x] 4.8 Soft delete de variacoes removidas (is_active=false)
  - [x] 4.9 Validacao Zod: nome obrigatorio, preco >= 0, min 1 variacao
- [x] Task 5: Edicao de precos em lote (AC: #6)
  - [x] 5.1 Criar `batch-price-editor.tsx`
  - [x] 5.2 Modo preco fixo e modo percentual
  - [x] 5.3 Preview tabela: preco atual → novo
  - [x] 5.4 Batch UPDATE em product_variants
  - [x] 5.5 Feedback de quantidade atualizada

## Dev Notes

### Schema SQL — Tabela `products`
```sql
CREATE TABLE public.products (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  account_id UUID NOT NULL REFERENCES public.accounts(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  description TEXT,
  category TEXT,          -- agrupamento opcional (ex: "temperos", "molhos", "carnes")
  unit TEXT NOT NULL DEFAULT 'un', -- 'un', 'kg', 'g', 'l'
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_products_account ON public.products(account_id);
CREATE INDEX idx_products_name ON public.products(account_id, name);
```

### Schema SQL — Tabela `product_variants`
```sql
CREATE TABLE public.product_variants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  product_id UUID NOT NULL REFERENCES public.products(id) ON DELETE CASCADE,
  account_id UUID NOT NULL REFERENCES public.accounts(id) ON DELETE CASCADE,
  name TEXT NOT NULL,             -- '500g', '1kg', '25kg'
  weight_grams INTEGER,           -- peso em gramas para sorting/calculo
  cost_price NUMERIC(10,2) NOT NULL,  -- preco de custo
  sell_price NUMERIC(10,2) NOT NULL,  -- preco de venda padrao
  sku TEXT,                        -- SKU opcional
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_product_variants_product ON public.product_variants(product_id);
CREATE INDEX idx_product_variants_account ON public.product_variants(account_id);
```

### RLS Policies — `products` (mesmo padrao para `product_variants`)
```sql
ALTER TABLE public.products ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.product_variants ENABLE ROW LEVEL SECURITY;

-- SELECT: usuario ve produtos das contas que pertence
CREATE POLICY "Users can view own account products"
  ON public.products FOR SELECT
  USING (account_id = ANY(get_user_account_ids()));

-- INSERT: admin ou vendedor
CREATE POLICY "Users can create products in own accounts"
  ON public.products FOR INSERT
  WITH CHECK (
    account_id = ANY(get_user_account_ids())
    AND get_user_role_in_account(account_id) IN ('admin', 'vendedor')
  );

-- UPDATE: admin ou vendedor
CREATE POLICY "Users can update products in own accounts"
  ON public.products FOR UPDATE
  USING (account_id = ANY(get_user_account_ids()))
  WITH CHECK (get_user_role_in_account(account_id) IN ('admin', 'vendedor'));

-- DELETE: apenas admin
CREATE POLICY "Admins can delete products"
  ON public.products FOR DELETE
  USING (
    account_id = ANY(get_user_account_ids())
    AND get_user_role_in_account(account_id) = 'admin'
  );

-- Repetir EXATAMENTE as mesmas policies para product_variants
-- (substituir "products" por "product_variants" nos nomes)
```

### Queries Supabase Client
```typescript
// Listar produtos com variacoes da conta ativa
const { data: products } = await supabase
  .from('products')
  .select('*, product_variants(*)')
  .eq('account_id', activeAccountId)
  .eq('is_active', true)
  .order('name');

// Busca por nome
const { data } = await supabase
  .from('products')
  .select('*, product_variants(*)')
  .eq('account_id', activeAccountId)
  .eq('is_active', true)
  .ilike('name', `%${searchTerm}%`);

// Criar produto com variacoes
// 1. Inserir produto
const { data: product } = await supabase
  .from('products')
  .insert({ account_id, name, description, category, unit })
  .select()
  .single();

// 2. Inserir variacoes
const variants = variantInputs.map(v => ({
  product_id: product.id,
  account_id,
  name: v.name,
  weight_grams: v.weight_grams,
  cost_price: v.cost_price,
  sell_price: v.sell_price,
  sku: v.sku,
}));
await supabase.from('product_variants').insert(variants);

// Edicao de preco em lote
await supabase
  .from('product_variants')
  .update({ sell_price: newPrice, updated_at: new Date().toISOString() })
  .in('id', selectedVariantIds);
```

### Modelo de Variacao — Exemplo Real
```
Produto: "Creme de Cebola"
├── Variacao: "500g"  → custo: R$8,00  → venda: R$12,00
├── Variacao: "1kg"   → custo: R$14,00 → venda: R$22,00
├── Variacao: "5kg"   → custo: R$60,00 → venda: R$95,00
└── Variacao: "25kg"  → custo: R$250,00 → venda: R$400,00

Produto: "Alho Descascado"
└── Variacao: "500g"  → custo: R$12,00 → venda: R$19,50
```

### Validacao Zod
```typescript
import { z } from 'zod';

const productVariantSchema = z.object({
  name: z.string().min(1, 'Nome da variacao obrigatorio'),
  weight_grams: z.number().int().positive().optional(),
  cost_price: z.number().min(0, 'Preco de custo deve ser >= 0'),
  sell_price: z.number().min(0, 'Preco de venda deve ser >= 0'),
  sku: z.string().optional(),
});

const productSchema = z.object({
  name: z.string().min(1, 'Nome do produto obrigatorio'),
  description: z.string().optional(),
  category: z.string().optional(),
  unit: z.enum(['un', 'kg', 'g', 'l']),
  variants: z.array(productVariantSchema).min(1, 'Pelo menos 1 variacao obrigatoria'),
});
```

### Arquivos Envolvidos
```
supabase/migrations/00005_products.sql
supabase/migrations/00006_rls_products.sql (ou incluir na 00004)
apps/web/src/app/(authenticated)/products/page.tsx
apps/web/src/app/(authenticated)/products/new/page.tsx
apps/web/src/app/(authenticated)/products/[id]/page.tsx
apps/web/src/components/products/product-form.tsx
apps/web/src/components/products/product-list.tsx
apps/web/src/components/products/batch-price-editor.tsx
packages/shared/src/utils/validation.ts (Zod schemas)
```

### Testing
- Testar CRUD completo de produto com variacoes (criar, ler, editar, desativar)
- Testar que produto criado na conta A nao aparece na conta B
- Testar busca por nome (parcial, case-insensitive)
- Testar filtro por categoria
- Testar edicao de precos em lote: selecionar 3 variacoes, aplicar novo preco, verificar que todas atualizaram
- Testar edicao por percentual: +10% em preco de R$100 → R$110
- Testar validacao: tentar criar produto sem nome → erro
- Testar validacao: tentar criar produto sem variacoes → erro
- Testar soft delete: desativar produto → nao aparece na lista → dados ainda existem no banco
- Testar RLS: vendedor pode criar/editar, entregador nao pode
- Testar que ~200 produtos carregam em < 2s (NFR-06)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-08 | 1.0 | Story created from PRD | River (SM) |
| 2026-06-10 | 1.1 | Validated (GO). Added Scope, Dependencies, Complexity, Risks. Status Draft → Ready | Pax (PO) |
| 2026-06-10 | 1.2 | Tasks 1-5 implemented. Migrations + RLS + list + form + batch editor. Build+lint pass. | Dex (Dev) |
| 2026-06-10 | 1.3 | QA Gate: CONCERNS. Fixed TS type error in [id]/page.tsx. Tests as tech debt. Status InProgress → Done | Quinn (QA) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### Completion Notes List
- Zod 4.4 installed for validation
- shadcn/ui Badge and Dialog components added
- Zod import uses `zod/v4` (Zod 4 breaking change)
- React 19 lint: avoided setState in useEffect body, used async load pattern with cleanup
- Product form handles create + edit with variant upsert and soft-delete of removed variants
- Batch price editor supports fixed price and percentage modes with preview
- `pnpm build` passes. `pnpm lint` passes.
- Migrations applied to Supabase Cloud

### File List
| File | Action | Description |
|------|--------|-------------|
| `supabase/migrations/00005_products.sql` | Created | products + product_variants tables |
| `supabase/migrations/00006_rls_products.sql` | Created | RLS policies for products |
| `apps/web/src/lib/validations/product.ts` | Created | Zod schemas for product + variant |
| `apps/web/src/app/(authenticated)/products/page.tsx` | Modified | Product list page |
| `apps/web/src/app/(authenticated)/products/new/page.tsx` | Created | New product page |
| `apps/web/src/app/(authenticated)/products/[id]/page.tsx` | Created | Edit product page |
| `apps/web/src/components/products/product-list.tsx` | Created | Product list with search/filter/expand |
| `apps/web/src/components/products/product-form.tsx` | Created | Product form with inline variants |
| `apps/web/src/components/products/batch-price-editor.tsx` | Created | Batch price update component |
| `apps/web/src/components/ui/badge.tsx` | Created | shadcn/ui Badge |
| `apps/web/src/components/ui/dialog.tsx` | Created | shadcn/ui Dialog |
