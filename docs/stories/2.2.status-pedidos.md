# Story 2.2: Status Granulares de Pedido

## Status
InProgress

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "manual-review"]

## Story
**As a** Tiago,
**I want** que cada pedido tenha status claros (Lancado, Confirmado, Carregado, Entregue) com badges visuais coloridos,
**so that** eu saiba exatamente em que estagio cada pedido esta sem confusao.

## Scope

### IN
- Componente OrderStatusBadge com cores por status
- Function PL/pgSQL `transition_order_status` com maquina de estados
- Function PL/pgSQL `batch_transition_status` para transicao em lote
- Lista de pedidos com filtros por status (chips), busca por cliente
- Pagina de detalhe do pedido com botoes de transicao
- ActionBar sticky para acoes em lote (selecao com checkboxes)
- Supabase Realtime para updates de status em tempo real
- Enums de status no shared package

### OUT
- Impressao termica de pedidos (Story 2.4)
- Devolucao parcial (Story 2.5)
- Cancelamento com motivo obrigatorio (futuro)
- Historico/auditoria de transicoes (futuro)

## Dependencies
- **Story 2.1** (Lancamento de Pedidos) — COMPLETED. orders, order_items, create_order, update_order

## Complexity
- **Estimate:** L (Large)
- **Points:** 8
- **Rationale:** 2 functions PL/pgSQL + 4 componentes + 2 paginas + Realtime subscription. Complexidade na maquina de estados (transicoes forward/reverse/cancelamento) e na UI de selecao em lote.

## Risks
- **R1:** Realtime subscription pode gerar re-renders excessivos com muitos pedidos. **Mitigacao:** Filtrar apenas updates de status, nao re-fetch completo.
- **R2:** Transicao em lote pode falhar parcialmente (alguns pedidos em status invalido). **Mitigacao:** batch_transition retorna resultado por pedido, UI mostra sucesso/falha individual.
- **R3:** Busca por nome de cliente via `ilike` em join pode ser lenta. **Mitigacao:** LIMIT + index em clients.name ja existe.

## Acceptance Criteria
1. Campo `status` no pedido com valores: `lancado`, `confirmado`, `carregado`, `entregue` (e `cancelado`)
2. Transicoes permitidas: lancado->confirmado, confirmado->carregado, carregado->entregue
3. Transicao reversa permitida: qualquer status pode voltar ao anterior (exceto entregue->lancado direto)
4. Filtro de pedidos por status na lista (chips de filtro horizontal scrollable)
5. Badges visuais coloridos por status: Lancado=cinza, Confirmado=azul, Carregado=amarelo, Entregue=verde
6. Confirmacao em lote: selecionar varios pedidos e confirmar/carregar de uma vez
7. Timestamps automaticos: `confirmed_at`, `loaded_at`, `delivered_at` preenchidos na transicao

## Tasks / Subtasks
- [x] Task 1: Implementar componente OrderStatusBadge (AC: #5)
  - [x] 1.1 Criar `apps/web/src/components/orders/order-status-badge.tsx`
  - [x] 1.2 Badge pill shape com cor de fundo + texto + borda conforme status
  - [x] 1.3 Cores: lancado (gray), confirmado (blue), carregado (yellow), entregue (green), cancelado (red)
  - [x] 1.4 Acessibilidade: cor + texto (nunca apenas cor)

- [x] Task 2: Criar funcao de transicao de status no Supabase (AC: #1, #2, #3, #7)
  - [x] 2.1 Function `transition_order_status(p_order_id, p_new_status)` PL/pgSQL
  - [x] 2.2 Validacao de transicoes via JSONB map (forward + reverse + cancelamento)
  - [x] 2.3 Timestamps automaticos: confirmed_at, loaded_at, delivered_at, cancelled_at
  - [x] 2.4 Limpa timestamps ao reverter
  - [x] 2.5 Migration `00012_order_status_transition.sql`

- [x] Task 3: Criar funcao de transicao em lote (AC: #6)
  - [x] 3.1 Function `batch_transition_status(p_order_ids UUID[], p_new_status)` PL/pgSQL
  - [x] 3.2 Loop chamando transition_order_status por pedido com error handling individual
  - [x] 3.3 Retorna JSONB array com {id, success, error?} por pedido

- [x] Task 4: Implementar lista de pedidos com filtros (AC: #4)
  - [x] 4.1 `order-list.tsx` com ChipFilter horizontal scrollable
  - [x] 4.2 Filtros: Todos, Lancado, Confirmado, Carregado, Entregue, Cancelado
  - [x] 4.3 Busca por nome do cliente ou numero do pedido (client-side)
  - [x] 4.4 Cards com checkbox, nome cliente, #pedido, valor, pgto badge, status badge, contagem itens
  - [x] 4.5 Ordenacao por created_at DESC

- [x] Task 5: Implementar pagina Lista de Pedidos (AC: #4, #6)
  - [x] 5.1 `orders/page.tsx` atualizado com OrderList
  - [x] 5.2 Integra OrderList com filtros e busca
  - [ ] 5.3 Badge multi-conta (RP/TI/VO) — diferido para quando multi-conta filtro estiver implementado
  - [x] 5.4 Tap no card navega para detalhe
  - [x] 5.5 FAB (+) para novo pedido

- [x] Task 6: Implementar ActionBar de selecao em lote (AC: #6)
  - [x] 6.1 `order-action-bar.tsx` sticky no bottom
  - [x] 6.2 Mostra contagem + botoes Confirmar/Carregar/Entregar
  - [x] 6.3 Botoes habilitados apenas se todos selecionados permitem a transicao
  - [x] 6.4 Chama batch_transition_status e mostra resultado
  - [x] 6.5 Auto-dismiss apos 1.5s e reload

- [x] Task 7: Implementar pagina de detalhe do pedido (AC: #1, #2, #3)
  - [x] 7.1 `orders/[id]/page.tsx` com info completa
  - [x] 7.2 Status badge, lista de itens, totais, pagamento, notas, timestamps
  - [x] 7.3 Botao de avancar status (forward)
  - [x] 7.4 Botao de reverter status (backward)
  - [x] 7.5 Botao Editar (link para edit page)

- [x] Task 8: Implementar Realtime para atualizacao de status (AC: #1)
  - [x] 8.1 Supabase Realtime subscription na order-list
  - [x] 8.2 Canal postgres_changes filtrado por account_id
  - [x] 8.3 Status badge atualiza sem refresh

- [ ] Task 9: Testes (AC: all)
  - [ ] 9.1 Unit test: transicoes de status validas e invalidas
  - [ ] 9.2 Unit test: timestamps preenchidos corretamente
  - [ ] 9.3 Unit test: OrderStatusBadge renderiza cores corretas
  - [ ] 9.4 Integration test: transicao em lote

## Dev Notes

### Status Machine

```
                      +-> cancelado (de qualquer status)
                      |
lancado <-> confirmado <-> carregado <-> entregue
  (cinza)    (azul)        (amarelo)     (verde)
```

Transicoes validas (forward): lancado->confirmado, confirmado->carregado, carregado->entregue
Transicoes validas (reverse): confirmado->lancado, carregado->confirmado, entregue->carregado
NAO permitido: entregue->lancado (pular 2 niveis)

### Supabase Function: `transition_order_status`

```sql
CREATE OR REPLACE FUNCTION public.transition_order_status(
  p_order_id UUID,
  p_new_status TEXT
) RETURNS VOID AS $$
DECLARE
  v_current_status TEXT;
  v_valid_transitions JSONB := '{
    "lancado": ["confirmado", "cancelado"],
    "confirmado": ["lancado", "carregado", "cancelado"],
    "carregado": ["confirmado", "entregue", "cancelado"],
    "entregue": ["carregado"]
  }'::JSONB;
BEGIN
  SELECT status INTO v_current_status FROM public.orders WHERE id = p_order_id;

  IF NOT (v_valid_transitions->v_current_status) ? p_new_status THEN
    RAISE EXCEPTION 'Invalid transition from % to %', v_current_status, p_new_status;
  END IF;

  UPDATE public.orders SET
    status = p_new_status,
    confirmed_at = CASE WHEN p_new_status = 'confirmado' THEN now()
                        WHEN p_new_status = 'lancado' THEN NULL
                        ELSE confirmed_at END,
    loaded_at = CASE WHEN p_new_status = 'carregado' THEN now()
                     WHEN p_new_status IN ('lancado', 'confirmado') THEN NULL
                     ELSE loaded_at END,
    delivered_at = CASE WHEN p_new_status = 'entregue' THEN now()
                       WHEN p_new_status IN ('lancado', 'confirmado', 'carregado') THEN NULL
                       ELSE delivered_at END,
    cancelled_at = CASE WHEN p_new_status = 'cancelado' THEN now() ELSE cancelled_at END,
    updated_at = now()
  WHERE id = p_order_id;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

### Status Badge Colors (UX Spec)

```
lancado:    bg: gray-100,   text: gray-700,   border: gray-300
confirmado: bg: blue-100,   text: blue-800,   border: blue-300
carregado:  bg: yellow-100, text: yellow-800, border: yellow-300
entregue:   bg: green-100,  text: green-800,  border: green-300
cancelado:  bg: red-100,    text: red-800,    border: red-300
```

### Enums (Shared Package)

```typescript
// packages/shared/src/types/enums.ts
export const OrderStatus = {
  LANCADO: 'lancado',
  CONFIRMADO: 'confirmado',
  CARREGADO: 'carregado',
  ENTREGUE: 'entregue',
  CANCELADO: 'cancelado',
} as const;

export type OrderStatusType = typeof OrderStatus[keyof typeof OrderStatus];

export const STATUS_COLORS: Record<OrderStatusType, { bg: string; text: string; border: string }> = {
  lancado:    { bg: 'bg-gray-100',   text: 'text-gray-700',   border: 'border-gray-300' },
  confirmado: { bg: 'bg-blue-100',   text: 'text-blue-800',   border: 'border-blue-300' },
  carregado:  { bg: 'bg-yellow-100', text: 'text-yellow-800', border: 'border-yellow-300' },
  entregue:   { bg: 'bg-green-100',  text: 'text-green-800',  border: 'border-green-300' },
  cancelado:  { bg: 'bg-red-100',    text: 'text-red-800',    border: 'border-red-300' },
};
```

### Realtime Subscription

```typescript
// In orders list page
supabase
  .channel('orders-realtime')
  .on('postgres_changes', {
    event: 'UPDATE',
    schema: 'public',
    table: 'orders',
    filter: `account_id=eq.${activeAccountId}`
  }, (payload) => {
    // Update the order in local state
    updateOrderInList(payload.new);
  })
  .subscribe();
```

### Conta Badge Shortcodes

Para a lista multi-conta, badges de conta usam iniciais com cores:
- `RP` = Distrial Rio Preto (#3B82F6 azul)
- `TI` = Tiago (#8B5CF6 roxo)
- `VO` = Votoporanga (#F97316 laranja)

### Folder Structure

```
apps/web/src/
  app/(authenticated)/orders/
    page.tsx              -- LISTA DE PEDIDOS (esta story)
    [id]/page.tsx         -- DETALHE DO PEDIDO (esta story)
  components/orders/
    order-list.tsx        -- lista com cards e checkboxes
    order-status-badge.tsx -- badge de status colorido
    order-action-bar.tsx  -- barra de acoes em lote
  components/ui/
    chip-filter.tsx       -- filtro com chips horizontais
```

### Supabase Query: Listar Pedidos com Filtro

```typescript
let query = supabase
  .from('orders')
  .select('*, client:clients(name), items:order_items(count)')
  .order('created_at', { ascending: false });

// Filtro por conta (se nao for "Todas")
if (accountId !== 'all') {
  query = query.eq('account_id', accountId);
}

// Filtro por status
if (statusFilter !== 'all') {
  query = query.eq('status', statusFilter);
}

// Busca por nome de cliente
if (searchTerm) {
  query = query.ilike('client.name', `%${searchTerm}%`);
}
```

### Testing

- **Unit tests** (`tests/unit/order-status.test.ts`):
  - Transicoes forward validas (lancado->confirmado, etc.)
  - Transicoes reverse validas (confirmado->lancado, etc.)
  - Transicoes invalidas rejeitadas (entregue->lancado)
  - Timestamps preenchidos na transicao forward
  - Timestamps limpos na transicao reverse
- **Unit tests** (`tests/unit/order-status-badge.test.ts`):
  - Renderiza cor correta para cada status
  - Texto do status legivel (nao apenas cor)
- **Integration tests** (`tests/integration/batch-transition.test.ts`):
  - Batch transition de 5 pedidos de lancado->confirmado
  - Batch com pedido invalido retorna erro parcial

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### Completion Notes List
- Added `@distriall/shared` as workspace dependency + `transpilePackages` in next.config
- ORDER_STATUS_LABELS, ORDER_STATUS_COLORS, VALID_TRANSITIONS added to shared enums
- React 19: loadOrders in useEffect wrapped with setTimeout to avoid sync setState
- Removed unused imports (VALID_TRANSITIONS in detail page, router, statuses var)
- batch_transition_status returns JSONB with per-order success/error for partial failure handling
- Realtime subscription on orders table filtered by account_id
- Task 5.3 (multi-account badge) deferred
- Task 9 (tests) documented as tech debt
- `pnpm build` passes. `pnpm lint` passes.

### File List
| File | Action | Description |
|------|--------|-------------|
| `supabase/migrations/00012_order_status_transition.sql` | Created | transition_order_status + batch_transition_status |
| `packages/shared/src/types/enums.ts` | Modified | Added ORDER_STATUS_LABELS, ORDER_STATUS_COLORS, VALID_TRANSITIONS |
| `apps/web/next.config.ts` | Modified | Added transpilePackages for @distriall/shared |
| `apps/web/package.json` | Modified | Added @distriall/shared workspace dependency |
| `apps/web/src/components/orders/order-status-badge.tsx` | Created | Colored status badge component |
| `apps/web/src/components/orders/order-list.tsx` | Created | Order list with filters, search, checkboxes, realtime |
| `apps/web/src/components/orders/order-action-bar.tsx` | Created | Batch action bar (confirm/load/deliver) |
| `apps/web/src/components/ui/chip-filter.tsx` | Created | Reusable horizontal chip filter |
| `apps/web/src/app/(authenticated)/orders/page.tsx` | Modified | Orders list page |
| `apps/web/src/app/(authenticated)/orders/[id]/page.tsx` | Created | Order detail page with status transitions |

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-08 | 1.0 | Story created from PRD | River (SM) |
| 2026-06-10 | 1.1 | Validated (GO). Added Scope, Dependencies, Complexity, Risks. Status Draft → Ready | Pax (PO) |
| 2026-06-10 | 1.2 | Tasks 1-8 implemented. Migration + 2 PL/pgSQL functions + enums + badge + list + detail + action-bar + realtime. Build+lint pass. | Dex (Dev) |
