# Story 1.2: Autenticacao e Perfis de Usuario

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "manual-review"]

## Story
**As a** Tiago (admin),
**I want** fazer login com meu usuario e ter meu perfil reconhecido com as permissoes corretas,
**so that** eu acesse apenas o que me pertence e o Diego acesse apenas o que lhe cabe.

## Acceptance Criteria
1. Login funcional com email + senha via Supabase Auth
2. Tabela `users` criada com campos: id, name, email, phone, role (admin, vendedor, entregador), avatar_url, is_active
3. Tiago criado como admin com acesso total
4. Diego criado como vendedor com acesso configuravel por conta
5. Joao criado como entregador com acesso restrito
6. Redirect pos-login para dashboard apropriado ao perfil (admin/vendedor → /dashboard, entregador → /driver)
7. Pagina de login com UI limpa e responsiva
8. Logout funcional com limpeza de sessao
9. Middleware de auth protegendo rotas autenticadas
10. RLS habilitado na tabela `users` — usuario so ve seu proprio perfil

## Tasks / Subtasks
- [ ] Task 1: Criar migration do schema `users` (AC: #2, #10)
  - [ ] 1.1 Criar migration `supabase/migrations/00001_users.sql` com tabela `users`
  - [ ] 1.2 Habilitar RLS na tabela `users`
  - [ ] 1.3 Criar policy: usuario pode ver apenas seu proprio perfil (SELECT WHERE id = auth.uid())
  - [ ] 1.4 Criar policy: usuario pode atualizar apenas seu proprio perfil (UPDATE WHERE id = auth.uid())
  - [ ] 1.5 Criar trigger `on_auth_user_created` que insere registro em `public.users` quando usuario e criado no Supabase Auth
  - [ ] 1.6 Aplicar migration local com `supabase db push` ou `supabase migration up`
- [ ] Task 2: Seed de usuarios iniciais (AC: #3, #4, #5)
  - [ ] 2.1 Criar `supabase/seed.sql` com INSERT dos 3 usuarios via Supabase Auth + public.users
  - [ ] 2.2 Tiago: email `tiago@distriall.com`, role `admin`
  - [ ] 2.3 Diego: email `diego@distriall.com`, role `vendedor`
  - [ ] 2.4 Joao: email `joao@distriall.com`, role `entregador`
  - [ ] 2.5 Documentar senhas default para desenvolvimento
- [ ] Task 3: Pagina de Login (AC: #1, #7)
  - [ ] 3.1 Criar `apps/web/src/app/login/page.tsx` com formulario email + senha
  - [ ] 3.2 Usar componentes shadcn/ui: Card, Input, Button, Label
  - [ ] 3.3 Implementar submit com `supabase.auth.signInWithPassword()`
  - [ ] 3.4 Exibir erro de login (credenciais invalidas) com toast ou mensagem inline
  - [ ] 3.5 Design mobile-first, centralizado, logo "Distriall" no topo
  - [ ] 3.6 Redirect apos login bem-sucedido (buscar role do usuario para decidir destino)
- [ ] Task 4: Middleware de autenticacao (AC: #6, #9)
  - [ ] 4.1 Criar `apps/web/src/middleware.ts` com Supabase auth middleware
  - [ ] 4.2 Proteger rotas `/(authenticated)/*` — redirecionar para `/login` se nao autenticado
  - [ ] 4.3 Proteger rota `/driver/*` — redirecionar para `/login` se nao autenticado
  - [ ] 4.4 Redirecionar `/login` para dashboard se ja autenticado
  - [ ] 4.5 Implementar logica de redirect por role: entregador → `/driver`, demais → `/dashboard`
- [ ] Task 5: Auth Provider e contexto (AC: #6, #8)
  - [ ] 5.1 Criar `apps/web/src/providers/auth-provider.tsx` com AuthContext
  - [ ] 5.2 Expor: `user`, `profile` (dados de public.users), `isLoading`, `signOut`
  - [ ] 5.3 Buscar perfil do usuario em `public.users` apos login
  - [ ] 5.4 Implementar `signOut()` com `supabase.auth.signOut()` + redirect para `/login`
  - [ ] 5.5 Adicionar provider no `apps/web/src/app/layout.tsx`
- [ ] Task 6: Layout autenticado com header (AC: #8)
  - [ ] 6.1 Criar `apps/web/src/app/(authenticated)/layout.tsx` que envolve paginas autenticadas
  - [ ] 6.2 Criar `apps/web/src/components/layout/header.tsx` com nome do usuario e botao de logout
  - [ ] 6.3 Criar `apps/web/src/components/layout/bottom-nav.tsx` navegacao mobile inferior (placeholder)
  - [ ] 6.4 O header deve ter espaco para o account switcher (implementado na Story 1.3)

## Dev Notes

### Schema SQL — Tabela `users`
```sql
CREATE TABLE public.users (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  email TEXT NOT NULL UNIQUE,
  phone TEXT,
  role TEXT NOT NULL CHECK (role IN ('admin', 'vendedor', 'entregador')),
  avatar_url TEXT,
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_users_email ON public.users(email);
CREATE INDEX idx_users_role ON public.users(role);
```

### RLS Policies — `users`
```sql
ALTER TABLE public.users ENABLE ROW LEVEL SECURITY;

-- Usuario ve apenas seu proprio perfil
CREATE POLICY "Users can view own profile"
  ON public.users FOR SELECT
  USING (id = auth.uid());

-- Usuario atualiza apenas seu proprio perfil
CREATE POLICY "Users can update own profile"
  ON public.users FOR UPDATE
  USING (id = auth.uid());
```

### Trigger: Auto-criar perfil ao registrar no Supabase Auth
```sql
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.users (id, name, email, role)
  VALUES (
    NEW.id,
    COALESCE(NEW.raw_user_meta_data->>'name', NEW.email),
    NEW.email,
    COALESCE(NEW.raw_user_meta_data->>'role', 'vendedor')
  );
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();
```

### Seed de Usuarios
```sql
-- Criar usuarios no Supabase Auth (usar supabase dashboard ou API admin)
-- Depois inserir perfis:
-- Tiago (admin)
INSERT INTO public.users (id, name, email, role) VALUES
  ('<tiago_uuid>', 'Tiago', 'tiago@distriall.com', 'admin');
-- Diego (vendedor)
INSERT INTO public.users (id, name, email, role) VALUES
  ('<diego_uuid>', 'Diego', 'diego@distriall.com', 'vendedor');
-- Joao (entregador)
INSERT INTO public.users (id, name, email, role) VALUES
  ('<joao_uuid>', 'Joao', 'joao@distriall.com', 'entregador');
```

### Middleware de Auth (Next.js)
```typescript
// apps/web/src/middleware.ts
import { createServerClient } from '@supabase/ssr';
import { NextResponse, type NextRequest } from 'next/server';

export async function middleware(request: NextRequest) {
  let response = NextResponse.next({ request });
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return request.cookies.getAll(); },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value, options }) => {
            request.cookies.set(name, value);
            response.cookies.set(name, value, options);
          });
        },
      },
    }
  );
  const { data: { user } } = await supabase.auth.getUser();
  const path = request.nextUrl.pathname;

  // Rotas protegidas: redirecionar para login se nao autenticado
  if (!user && (path.startsWith('/dashboard') || path.startsWith('/driver') || path.startsWith('/orders') || path.startsWith('/products') || path.startsWith('/clients') || path.startsWith('/financial') || path.startsWith('/stats') || path.startsWith('/settings'))) {
    return NextResponse.redirect(new URL('/login', request.url));
  }

  // Ja autenticado tentando acessar login: redirecionar para dashboard
  if (user && path === '/login') {
    // Buscar role para decidir redirect
    const { data: profile } = await supabase.from('users').select('role').eq('id', user.id).single();
    if (profile?.role === 'entregador') {
      return NextResponse.redirect(new URL('/driver', request.url));
    }
    return NextResponse.redirect(new URL('/dashboard', request.url));
  }

  return response;
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico|icons|manifest.json|sw.js|offline.html).*)'],
};
```

### Redirect por Role
| Role | Destino pos-login |
|------|-------------------|
| admin | `/dashboard` |
| vendedor | `/dashboard` |
| entregador | `/driver` |

### Arquivos Envolvidos
```
supabase/migrations/00001_users.sql
supabase/seed.sql
apps/web/src/app/login/page.tsx
apps/web/src/app/(authenticated)/layout.tsx
apps/web/src/middleware.ts
apps/web/src/providers/auth-provider.tsx
apps/web/src/components/layout/header.tsx
apps/web/src/components/layout/bottom-nav.tsx
```

### Testing
- Testar login com credenciais validas → redirect para dashboard
- Testar login com credenciais invalidas → mensagem de erro
- Testar login como entregador → redirect para /driver
- Testar acesso a rota protegida sem auth → redirect para /login
- Testar acesso a /login quando ja autenticado → redirect para dashboard
- Testar logout → sessao limpa, redirect para /login
- Testar RLS: usuario A nao consegue ver perfil do usuario B via Supabase client
- Testar trigger: ao criar usuario no Auth, perfil aparece em public.users

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-08 | 1.0 | Story created from PRD | River (SM) |
