# Story 2.1: Lancamento e Edicao de Pedidos

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "manual-review"]

## Story
**As a** Tiago ou Diego (vendedor),
**I want** lancar pedidos selecionando o cliente e adicionando produtos com quantidades,
**so that** o pedido fique registrado e pronto para confirmacao, com precos corretos por cliente carregados automaticamente.

## Acceptance Criteria
1. Tela "Novo Pedido": selecionar cliente via busca com autocomplete; ao selecionar, precos customizados sao carregados automaticamente
2. Adicionar produtos com quantidade via busca rapida por nome; quantidade com botoes +/- touch-friendly
3. Exibir total do pedido (faturamento e lucro estimado) em tempo real conforme itens sao adicionados/alterados
4. Salvar pedido com status "Lancado" vinculado a conta ativa e ao usuario que criou
5. Edicao de pedido em qualquer status (Lancado, Confirmado, Carregado) sem precisar excluir e relancar
6. Pedido vinculado a conta ativa e ao usuario que criou (created_by)
7. Forma de pagamento pre-selecionada com o padrao do cliente, editavel
8. Campo de observacoes (notas) opcional no pedido

## Tasks / Subtasks
- [ ] Task 1: Criar Supabase function `create_order` (AC: #1, #3, #4, #6)
  - [ ] 1.1 Implementar a function PL/pgSQL `create_order(p_account_id, p_client_id, p_items, p_payment_method, p_notes, p_delivery_date)` conforme architecture doc
  - [ ] 1.2 A function deve buscar `client_prices` para cada item e usar `COALESCE(custom_price, sell_price)` como preco unitario
  - [ ] 1.3 A function deve snapshot `product_name` e `variant_name` nos `order_items` (para historico imutavel)
  - [ ] 1.4 A function deve calcular e atualizar `subtotal`, `total_cost`, `profit`, `total` no `orders`
  - [ ] 1.5 Criar migration file `supabase/migrations/XXXXX_create_order_function.sql`
  - [ ] 1.6 Testar com dados de seed

- [ ] Task 2: Criar Zustand cart store (AC: #2, #3)
  - [ ] 2.1 Criar `apps/web/src/stores/cart-store.ts` com Zustand
  - [ ] 2.2 State: `client`, `items[]`, `paymentMethod`, `notes`, `deliveryDate`
  - [ ] 2.3 Actions: `setClient`, `addItem`, `removeItem`, `updateQuantity`, `clearCart`
  - [ ] 2.4 Computed: `subtotal`, `totalCost`, `estimatedProfit` (recalculados em tempo real)
  - [ ] 2.5 Ao `setClient`, buscar `client_prices` do Supabase e mesclar com precos padrao dos `product_variants`

- [ ] Task 3: Implementar componente de busca de clientes (AC: #1, #7)
  - [ ] 3.1 Criar `apps/web/src/components/orders/client-search.tsx`
  - [ ] 3.2 Autocomplete com debounce (300ms) buscando em `clients` da conta ativa
  - [ ] 3.3 Ao selecionar cliente, carregar precos customizados e forma de pagamento padrao
  - [ ] 3.4 Mostrar badge com quantidade de precos especiais do cliente
  - [ ] 3.5 Botao [+] para cadastro rapido de novo cliente inline

- [ ] Task 4: Implementar componente de busca e adicao de produtos (AC: #2)
  - [ ] 4.1 Criar `apps/web/src/components/orders/product-search.tsx`
  - [ ] 4.2 Autocomplete buscando em `products` + `product_variants` da conta ativa
  - [ ] 4.3 Ao selecionar, adicionar ao cart store com quantidade 1
  - [ ] 4.4 Mostrar variantes inline se produto tiver multiplas (ex: 500g, 1kg, 25kg)

- [ ] Task 5: Implementar componente de lista de itens do pedido (AC: #2, #3)
  - [ ] 5.1 Criar `apps/web/src/components/orders/order-item-list.tsx`
  - [ ] 5.2 Cada item: nome produto, variante, preco unitario, botoes [-] quantidade [+], subtotal da linha
  - [ ] 5.3 Botoes +/- com min-height 48px para touch-friendly
  - [ ] 5.4 Swipe left para remover item (ou botao X)

- [ ] Task 6: Implementar pagina Novo Pedido (AC: #1, #2, #3, #4, #7, #8)
  - [ ] 6.1 Criar `apps/web/src/app/(authenticated)/orders/new/page.tsx`
  - [ ] 6.2 Layout: ClientSearch > ProductSearch > OrderItemList > Resumo financeiro > PaymentMethod > Notes > Botao Salvar
  - [ ] 6.3 Resumo financeiro sticky no bottom: subtotal, custo, lucro estimado
  - [ ] 6.4 Ao salvar, chamar `supabase.rpc('create_order', {...})` com dados do cart store
  - [ ] 6.5 Apos salvar com sucesso: limpar cart, toast "Pedido salvo", redirect para lista de pedidos
  - [ ] 6.6 Tratamento de erro com toast informativo

- [ ] Task 7: Implementar pagina Editar Pedido (AC: #5)
  - [ ] 7.1 Criar `apps/web/src/app/(authenticated)/orders/[id]/edit/page.tsx`
  - [ ] 7.2 Carregar pedido existente com itens e popular o cart store
  - [ ] 7.3 Permitir edicao em qualquer status exceto `entregue` e `cancelado`
  - [ ] 7.4 Ao salvar, atualizar `order_items` (delete old + insert new) e recalcular totais no `orders`
  - [ ] 7.5 Criar Supabase function `update_order` para atomicidade

- [ ] Task 8: Implementar hook `use-orders` (AC: #4, #5, #6)
  - [ ] 8.1 Criar `apps/web/src/hooks/use-orders.ts`
  - [ ] 8.2 Functions: `createOrder`, `updateOrder`, `getOrder`, `listOrders`
  - [ ] 8.3 Integrar com Supabase client e RLS (filtro por conta ativa automatico)

- [ ] Task 9: Testes (AC: all)
  - [ ] 9.1 Unit tests para cart store (calculos de subtotal, profit)
  - [ ] 9.2 Unit tests para logica de preco customizado vs padrao
  - [ ] 9.3 Integration test: criar pedido com items e verificar totais

## Dev Notes

### Database Tables Envolvidas

**`orders`** (principal):
```sql
CREATE TABLE public.orders (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  account_id UUID NOT NULL REFERENCES public.accounts(id) ON DELETE CASCADE,
  client_id UUID NOT NULL REFERENCES public.clients(id),
  created_by UUID NOT NULL REFERENCES public.users(id),
  order_number SERIAL,
  status TEXT NOT NULL DEFAULT 'lancado'
    CHECK (status IN ('lancado', 'confirmado', 'carregado', 'entregue', 'cancelado')),
  payment_method TEXT CHECK (payment_method IN ('dinheiro', 'pix', 'boleto', 'vale', 'cartao', 'misto')),
  subtotal NUMERIC(12,2) NOT NULL DEFAULT 0,
  total_cost NUMERIC(12,2) NOT NULL DEFAULT 0,
  profit NUMERIC(12,2) NOT NULL DEFAULT 0,
  discount NUMERIC(10,2) NOT NULL DEFAULT 0,
  total NUMERIC(12,2) NOT NULL DEFAULT 0,
  notes TEXT,
  delivery_date DATE,
  delivered_at TIMESTAMPTZ,
  confirmed_at TIMESTAMPTZ,
  loaded_at TIMESTAMPTZ,
  cancelled_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

**`order_items`**:
```sql
CREATE TABLE public.order_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  order_id UUID NOT NULL REFERENCES public.orders(id) ON DELETE CASCADE,
  account_id UUID NOT NULL REFERENCES public.accounts(id) ON DELETE CASCADE,
  product_variant_id UUID NOT NULL REFERENCES public.product_variants(id),
  product_name TEXT NOT NULL,       -- snapshot at order time
  variant_name TEXT NOT NULL,       -- snapshot at order time
  quantity NUMERIC(10,3) NOT NULL,
  unit_price NUMERIC(10,2) NOT NULL, -- price at order time (custom or default)
  cost_price NUMERIC(10,2) NOT NULL, -- cost at order time
  total NUMERIC(12,2) NOT NULL,      -- quantity * unit_price
  total_cost NUMERIC(12,2) NOT NULL, -- quantity * cost_price
  is_returned BOOLEAN NOT NULL DEFAULT false,
  returned_quantity NUMERIC(10,3) DEFAULT 0,
  return_reason TEXT,
  returned_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

**`client_prices`** (para preco customizado):
```sql
CREATE TABLE public.client_prices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  account_id UUID NOT NULL REFERENCES public.accounts(id) ON DELETE CASCADE,
  client_id UUID NOT NULL REFERENCES public.clients(id) ON DELETE CASCADE,
  product_variant_id UUID NOT NULL REFERENCES public.product_variants(id) ON DELETE CASCADE,
  custom_price NUMERIC(10,2) NOT NULL,
  UNIQUE(client_id, product_variant_id)
);
```

### Supabase Function `create_order`

A function completa esta no architecture doc (secao 6.3). Pontos criticos:
- Usa `SECURITY DEFINER` para acessar `auth.uid()` como `created_by`
- Faz loop em `p_items` (JSONB array) para inserir `order_items`
- Para cada item, busca `client_prices.custom_price` e faz `COALESCE(custom_price, sell_price)`
- Snapshots `product_name` e `variant_name` no momento da criacao (imutavel)
- Retorna o UUID do pedido criado

### Logica de Preco por Cliente (Client-Side)

No cart store, ao selecionar cliente:
```typescript
// Buscar precos customizados
const { data: customPrices } = await supabase
  .from('client_prices')
  .select('product_variant_id, custom_price')
  .eq('client_id', clientId);

// Criar mapa de precos: variant_id -> price
const priceMap = new Map(customPrices.map(cp => [cp.product_variant_id, cp.custom_price]));

// Ao adicionar item ao carrinho, usar:
const unitPrice = priceMap.get(variantId) ?? variant.sell_price;
```

### RLS Policies (ja cobertas no Epic 1)

As orders seguem o padrao multi-tenant:
- SELECT: `account_id = ANY(get_user_account_ids())`
- INSERT: `account_id = ANY(get_user_account_ids()) AND role IN ('admin', 'vendedor')`
- UPDATE: mesma logica
- DELETE: admin only

### Folder Structure

```
apps/web/src/
  app/(authenticated)/orders/
    page.tsx           -- lista de pedidos (Story 2.2)
    new/page.tsx       -- NOVO PEDIDO (esta story)
    [id]/
      page.tsx         -- detalhe do pedido
      edit/page.tsx    -- EDITAR PEDIDO (esta story)
  components/orders/
    client-search.tsx  -- busca de cliente com autocomplete
    product-search.tsx -- busca de produto com autocomplete
    order-item-list.tsx -- lista de itens no carrinho
    order-form.tsx     -- form completo (wrapper)
  hooks/
    use-orders.ts      -- hook para CRUD de pedidos
  stores/
    cart-store.ts      -- Zustand store para carrinho
```

### UX Notes (da ux-spec)

- Botoes +/- quantidade: min-height 48px, touch-friendly
- Resumo financeiro sticky no bottom do formulario
- Busca de cliente com autocomplete e badge de precos especiais
- Forma de pagamento pre-selecionada do cadastro do cliente
- Botao salvar: "Salvar Pedido" (novo) / "Atualizar Pedido" (edicao)
- FAB (+) no bottom nav redireciona para esta pagina

### Supabase Queries

```typescript
// Listar produtos da conta com variantes
supabase.from('products')
  .select('*, product_variants(*)')
  .eq('account_id', activeAccountId)
  .eq('is_active', true)
  .order('name');

// Buscar cliente por nome (autocomplete)
supabase.from('clients')
  .select('*')
  .eq('account_id', activeAccountId)
  .ilike('name', `%${search}%`)
  .limit(10);

// Criar pedido
supabase.rpc('create_order', {
  p_account_id: activeAccountId,
  p_client_id: clientId,
  p_items: JSON.stringify(items),
  p_payment_method: paymentMethod,
  p_notes: notes,
  p_delivery_date: deliveryDate
});
```

### Testing

- **Unit tests** (`tests/unit/cart-store.test.ts`):
  - Adicionar item ao carrinho calcula subtotal corretamente
  - Remover item recalcula subtotal
  - Alterar quantidade recalcula subtotal e lucro
  - Preco customizado tem prioridade sobre preco padrao
  - Limpar carrinho reseta tudo
- **Unit tests** (`tests/unit/calculations.test.ts`):
  - Calculo de profit: `subtotal - total_cost`
  - Arredondamento a 2 casas decimais
- **Integration tests** (`tests/integration/order-flow.test.ts`):
  - Criar pedido via `create_order` function e verificar totais no banco
  - Criar pedido com precos customizados e verificar que foram usados
  - Editar pedido e verificar recalculo de totais

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-08 | 1.0 | Story created from PRD | River (SM) |
