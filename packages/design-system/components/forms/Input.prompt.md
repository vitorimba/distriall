Text input; pair with `<Field>` for label and inline error.

```jsx
<Input placeholder="Buscar cliente..." icon="search" />
<Input prefix="R$" placeholder="0,00" />
```

Props: `invalid`, `icon` (Lucide), `prefix` ("R$"), plus native input props.
