The container for every grouped block of content (forms sections, charts, tables).

```jsx
<Card title="Itens do pedido" action={<Button size="sm" variant="ghost" icon="plus">Adicionar</Button>}>
  ...
</Card>
```

Use `padded={false}` when the body is a `DataTable` (flush edges).
