Icon-only square button for row actions and header tools.

```jsx
<IconButton icon="pencil" label="Editar cliente" />
```

Variants: `ghost | outline | primary | danger`. Always pass `label` (becomes aria-label + tooltip).
