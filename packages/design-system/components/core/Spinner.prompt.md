Spinner indeterminado para carregamentos curtos sem layout conhecido; se a estrutura é conhecida, prefira Skeleton.

```jsx
<Spinner />
<Spinner size={24} />
```

- Já embutido em `Button loading` — não adicionar outro.
- Centralizar com flex no container quando for o único conteúdo.
