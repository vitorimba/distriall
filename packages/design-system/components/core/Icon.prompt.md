Lucide stroke icon by kebab-case name; use for ALL iconography (never hand-draw SVGs).

```jsx
<Icon name="truck" size={18} />
```

Requires `<script src="https://unpkg.com/lucide@0.460.0/dist/umd/lucide.min.js"></script>` on the page. Inherits `currentColor`.
