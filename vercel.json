{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "source ~/.nvm/nvm.sh && nvm install 20 && nvm use 20 && npm i -g pnpm@9 && pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
